# Example environment file for docker-compose
# Copy to `.env` and customize as needed

# MySQL configuration
MYSQL_DATABASE=election
MYSQL_ROOT_PASSWORD=mydb9am
MYSQL_USER=root
MYSQL_PASSWORD=mydb9am

# Database connection for Spring Boot
DB_HOST=db
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=mydb9am

# Ports (host:container)
BACKEND_PORT=8080
FRONTEND_PORT=3000
MYSQL_PORT=3306

# API URL for frontend build - point to backend service name and API base
REACT_APP_API_URL=http://backend:8080/api

SPRING_PROFILES_ACTIVE=default

# Optional: Docker Hub image names (if pushing images to registry)
#BACKEND_IMAGE=<your_dockerhub_username>/ov-backend:latest
#FRONTEND_IMAGE=<your_dockerhub_username>/ov-frontend:latest
